# Default Port 3010 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Make `3010` the repository default service port everywhere the project declares or documents the default.

**Architecture:** Keep the change narrow. Update the runtime default in config, align setup-wizard defaults and user-facing health-check text, then update tests and README examples to the same default. Preserve `.env` overrides.

**Tech Stack:** Node.js ESM, built-in Node test runner, systemd user service, Markdown docs

---

### Task 1: Update tests for the new default port

**Files:**
- Modify: `test/init-guide.test.js`

**Step 1: Write the failing test**

Change the existing assertions that expect `PORT=3000` so they expect `PORT=3010`.

**Step 2: Run test to verify it fails**

Run: `node --test test/init-guide.test.js`
Expected: FAIL because setup guidance still uses `3000`.

**Step 3: Write minimal implementation**

Update the production defaults in setup guidance and config to use `3010`.

**Step 4: Run test to verify it passes**

Run: `node --test test/init-guide.test.js`
Expected: PASS

### Task 2: Update runtime defaults and setup guidance

**Files:**
- Modify: `src/config.js`
- Modify: `src/application/init-guide.js`
- Modify: `src/index.js`

**Step 1: Change runtime defaults**

Set the default configured port to `3010`.

**Step 2: Change setup text**

Update setup wizard defaults, checklist text, and the service-install health-check hint to use `3010`.

**Step 3: Run focused tests**

Run: `node --test test/init-guide.test.js test/config-cli-provider.test.js`
Expected: PASS

### Task 3: Update README examples

**Files:**
- Modify: `README.md`

**Step 1: Change documented default port**

Update example `curl` commands and `.env` snippet from `3000` to `3010`.

**Step 2: Verify consistency**

Search for remaining repository-owned `3000` examples and confirm any remaining matches are historical docs or third-party content.

Run: `grep -RIn "3000" src README.md test package.json . 2>/dev/null | head -200`
Expected: Only intentionally untouched historical/generated/vendor matches remain.

### Task 4: Verify end to end

**Files:**
- Modify: none

**Step 1: Run targeted tests**

Run: `node --test test/init-guide.test.js test/config-cli-provider.test.js`
Expected: PASS

**Step 2: Restart service**

Run: `npm run service:restart`
Expected: systemd user service restarts successfully.

**Step 3: Verify health**

Run: `curl -fsS http://127.0.0.1:3010/healthz`
Expected: JSON payload with `"ok": true`.
